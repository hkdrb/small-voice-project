import pandas as pd
import random
import os
import shutil
from datetime import datetime, timedelta

import sys

# Add project root to path
sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

# --- Configuration ---
NUM_ROWS_VALUES = 1000
NUM_ROWS_OTHERS = 1000
OUTPUT_DIR = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "outputs", "test_data")

# --- Utility Functions ---
def generate_random_date():
    start_date = datetime.now() - timedelta(days=90)
    random_days = random.randint(0, 90)
    random_seconds = random.randint(0, 86400)
    dt = start_date + timedelta(days=random_days, seconds=random_seconds)
    return dt.strftime("%Y/%m/%d %H:%M")

def generate_combinatorial_sentence(parts):
    """
    parts: list of lists
    """
    sentence = ""
    for part_list in parts:
        if not part_list: continue
        fragment = random.choice(part_list)
        if fragment:
            sentence += fragment
    return sentence

def generate_unique_sentence_list(generator_func, count, max_retries=10):
    """Generates a list of unique sentences."""
    seen = set()
    result = []
    
    for i in range(count):
        sentence = generator_func()
        retries = 0
        while sentence in seen and retries < max_retries:
            sentence = generator_func()
            retries += 1
        
        # If still duplicate after retries, force uniqueness
        if sentence in seen:
            sentence += f" ({i+1})"
            
        seen.add(sentence)
        result.append(sentence)
    
    return result

# --- Sentence Parts Definitions ---

INTROS = [
    "正直に言うと、", "最近思うのですが、", "以前から気になっていたのですが、", "あくまで個人的な意見ですが、", 
    "担当者として感じることですが、", "現場の声として言わせてください。", "ちょっと思ったんですけど、", 
    "改善提案なのですが、", "今のチームの課題として、", "率直な感想ですが、", "ふと思ったのですが、",
    "議論の余地はあると思いますが、", "個人的には、", "チーム全体として、", "少し気になっている点として、",
    "", "", "", "", "", ""
]

CLOSINGS = [
    "と思います。", "と感じています。", "という気がします。", "のではないでしょうか。", "をご検討ください。", 
    "をお願いしたいです。", "だと嬉しいです。", "はなんとかならないでしょうか。", "が急務です。", 
    "と強く感じます。", "ですね。", "につきると思います。", "このままだとマズイです。", 
    "改善されることを期待しています。", "と確信しています。", "と願っています。", 
    "の検討をお願いします。", "だと助かります。", "", "", ""
]

# 1. Project Management
PROJ_SUBJECTS = [
    "朝会の時間が", "スプリントプランニングが", "仕様書の更新頻度が", "タスクの粒度が", "JIRAのチケット管理が", 
    "プロダクトオーナーの意思決定が", "要件定義の詰めが", "納期のプレッシャーが", "関係部署との調整が", 
    "会議の回数が", "ドキュメント管理が", "リソース配分が", "バックログの優先順位が", "スプリントレビューの進行が",
    "Slackの通知量が", "情報の透明性が", "意思決定のスピードが", "KPTの質が", "心理的安全性が"
]

PROJ_ACTIONS = [
    "長すぎて業務を圧迫している", "曖昧なまま進んでいて不安だ", "頻繁に変わりすぎて追いつけない", 
    "荒すぎて見積もりができない", "煩雑すぎて更新が漏れる", "遅くて開発が止まることが多い", 
    "甘くて手戻りばかり発生する", "厳しすぎて品質が犠牲になっている", "うまくいかずコンフリクトばかり起きる", 
    "多すぎて作業時間が確保できない", "形骸化していて誰も見ていない", "適切でなく特定の人に負荷が偏っている",
    "不明瞭で何をすべきか迷う", "スムーズでなく時間がもったいない", "多すぎて集中できない",
    "低くて噂話で広まっている", "遅くてチャンスを逃している"
]

PROJ_REASONS = [
    "ので、もっと効率化してほしい", "ため、ルールの見直しが必要です", "から、プロセスを改善すべきです", 
    "ので、ツールを導入して自動化したい", "ため、一度チームで話し合う時間をください", 
    "という現状を打破したい", "ので、上層部の理解が必要です", "ため、専任の担当者をつけてほしい",
    "から、ガイドラインを策定すべきです", "ので、不要なものは廃止しましょう", "ため、改善MTGを開きたいです",
    "ので、リーダーの判断を仰ぎたい", "から、ワークフローを整理したい"
]

PROJ_ACTIONS_POS = [
    "改善されて業務効率が上がっている", "明確になって動きやすくなった", "適切で見積もりがしやすくなった", 
    "スムーズでストレスが減った", "透明性が高くて安心感がある", "迅速で開発スピードが上がった", 
    "活発で良い議論ができている", "柔軟で助かっている"
]

PROJ_REASONS_POS = [
    "ので、この調子で進めてほしい", "ため、他のチームにも展開したいです", "から、非常に助かっています", 
    "ので、モチベーションが上がりました", "ため、感謝しています", "という良いサイクルが生まれています",
    "ので、継続していきましょう"
]

PROJ_ACTIONS_NEUTRAL = [
    "まだ判断が難しい", "可もなく不可もなくといったところだ", "様子を見る必要がある",
    "議論の余地がある", "一長一短がある", "影響は限定的のようだ",
    "今のところ大きな問題はない", "もう少しデータが必要だ"
]

PROJ_REASONS_NEUTRAL = [
    "ので、しばらく静観します", "ため、次のスプリントで再度確認します",
    "から、他者の意見も聞いてみたいです", "ので、急いで結論を出す必要はないでしょう",
    "ため、現状維持で様子を見ましょう", "という点について、どう思いますか？"
]

# 2. Dev Environment
DEV_SUBJECTS = [
    "ローカル開発環境のDockerが", "CI/CDパイプラインが", "検証環境のデータが", "支給されているPCのメモリが", 
    "VPNの接続が", "社内プロキシの設定が", "AWSの権限周りが", "テスト自動化の仕組みが", 
    "使用しているライブラリのバージョンが", "GitHub Copilotの導入が", "モニターの解像度が", 
    "オフィスチェアの座り心地が", "デプロイフローが", "ログ監視ツールが", "開発用サーバーのスペックが",
    "IDEのライセンス数が", "リモートアクセスの手順が"
]

DEV_ACTIONS = [
    "重すぎて開発効率が悪い", "遅すぎて待ち時間が無駄だ", "古すぎて本番と乖離している", 
    "不足していてChromeを開くと固まる", "不安定で頻繁に切断される", "複雑すぎて新しいツールの導入が阻まれる", 
    "厳しすぎて作業が円滑に進まない", "脆弱でよく壊れる", "古すぎてセキュリティリスクがある", 
    "遅れていて競合に負けている", "低すぎて作業領域が狭い", "悪くて腰痛が悪化した", 
    "手動作業が多くてミスが怖い", "使いにくくて調査に時間がかかる", "低すぎてビルドに時間がかかる",
    "足りなくて順番待ちが発生している"
]

DEV_REASONS = [
    "ので、M2/M3 Macへのリプレイスをお願いします", "ため、マシンスペックの底上げが必要です", 
    "から、クラウドベースの開発環境へ移行したい", "ので、ネットワークインフラの増強が急務です", 
    "ため、最新ツールの導入許可をください", "という理由でモチベーションが下がっています", 
    "ので、インフラチームのリソースを増やしてほしい", "ため、予算を確保してください",
    "ので、早急に対応をお願いします", "から、試験的にでも導入したい"
]

DEV_ACTIONS_POS = [
    "快適で開発に集中できる", "高速で待ち時間がほぼない", "最新で使いやすい", 
    "安定していて信頼できる", "十分でストレスがない", "便利で生産性が向上した", 
    "充実していて学習しやすい", "柔軟でリモートワークがしやすい"
]

DEV_REASONS_POS = [
    "ので、会社に感謝しています", "ため、知人にも紹介したい環境です", "から、長く働きたいと思えます", 
    "ので、さらに高みを目指せそうです", "ため、素晴らしい投資だと思います", "という環境はなかなかないです",
    "ので、この環境を維持してほしい"
]

DEV_ACTIONS_NEUTRAL = [
    "普通に使えている", "慣れれば問題ないレベルだ", "最低限の要件は満たしている",
    "特に不満はないが感動もない", "標準的だと思う", "時々気になる程度だ",
    "マニュアル通りに動いている", "許容範囲内だ"
]

DEV_REASONS_NEUTRAL = [
    "ので、このままで良いと思います", "ため、特に要望はありません",
    "から、慣れの問題かもしれません", "ので、もう少し使い込んでみます",
    "ため、今のところ支障はありません", "という点は理解しています"
]

# 3. Tech Quality
TECH_SUBJECTS = [
    "レガシーコードの", "APIのレスポンス速度が", "ユニットテストのカバレッジが", "エラーハンドリングが", 
    "データベースのスキーマ設計が", "フロントエンドのコンポーネント設計が", "変数の命名規則が", 
    "ログの出力内容が", "コードレビューの", "技術的負債の", "ドキュメントの", "オンボーディング資料の",
    "ライブラリの依存関係が", "マイクロサービスの分割粒度が", "例外処理の統一感が", "SQLのパフォーマンスが"
]

TECH_ACTIONS = [
    "複雑さが増してメンテナンス不能になりつつある", "遅すぎてUXを損なっている", "低すぎて変更を加えるのが怖い", 
    "統一されておらずデバッグが困難だ", "破綻していてパフォーマンスが出ない", "カオス状態で再利用性が皆無だ", 
    "バラバラで可読性が著しく低い", "不十分で障害調査に役立たない", "基準が曖昧で属人化している", 
    "蓄積されすぎて爆発寸前だ", "陳腐化していて嘘ばかり書いてある", "欠如していて新人が立ち上がれない",
    "複雑に絡み合ってアップデートできない", "不適切で運用負荷が高い", "甘くてデータ整合性が取れない"
]

TECH_REASONS = [
    "ので、リファクタリング期間を設けてほしい", "ため、技術顧問を招聘して指導を仰ぎたい", 
    "から、静的解析ツールの導入を徹底すべきです", "ので、勉強会を開催して意識合わせをしたい", 
    "ため、設計から見直す必要があります", "という状況を放置すべきではありません", 
    "ので、品質保証のプロセスを強化しましょう", "ため、ドキュメントの整備をタスク化してください",
    "ので、Lintツールの導入を検討したい", "から、ペアプロをもっと導入したい"
]

TECH_ACTIONS_POS = [
    "綺麗に整理されていて読みやすい", "高速でユーザー体験が良い", "網羅されていて変更が容易だ", 
    "堅牢で障害が少ない", "洗練されていて再利用性が高い", "統一感があり理解しやすい", 
    "適切で技術的な負債が少ない", "最新技術が取り入れられていて刺激的だ"
]

TECH_REASONS_POS = [
    "ので、開発していて楽しい", "ため、新人のキャッチアップも早いです", "から、品質には自信があります", 
    "ので、デプロイするのが楽しみです", "ため、エンジニアとしての成長を感じます", 
    "という文化は素晴らしいです",    "ので、この品質を保っていきましょう"
]

TECH_ACTIONS_NEUTRAL = [
    "複雑だが理解可能なレベルだ", "パフォーマンスは許容範囲内だ", "古い記述もあるが動作はしている",
    "一般的と言える構成だ", "可読性は普通だと思う", "特に大きな技術的負債とは感じない",
    "リファクタリングの優先度は中程度だ", "現状の規模なら妥当だ"
]

TECH_REASONS_NEUTRAL = [
    "ので、急いで直すほどではない", "ため、折を見て対応すれば良い",
    "から、現状維持で進めましょう", "ので、余裕がある時に見直します",
    "ため、大きなリスクにはならないでしょう", "という認識で合っていますか？"
]

# 4. Values (Combinatorial Explosion needed here)
VALUES_THEMES = [
    "私たちは常に誠実に行動することを誓います。", "プロフェッショナルとして品質に責任を持ちます。", 
    "チームワークで困難を乗り越えます。", "失敗を恐れずに新しいことに挑戦します。", 
    "常にお客様の成功を第一に考えます。", "圧倒的なスピードで価値を提供します。", 
    "互いに感謝し、リスペクトを持ちます。", "仕事を楽しむ心を忘れません。", 
    "データと事実に基づいて論理的に思考します。", "現状に満足せず、継続的に学習します。", 
    "心理的安全性を確保し、本音で議論します。", "情報を隠さず、オープンに共有します。", 
    "誰かに言われる前に、自律的に行動します。", "見返りを求めず、まずは自分から与えます。", 
    "心身の健康を第一優先にします。", "当事者意識を持ち、最後までやり抜きます。", 
    "透明性の高い意思決定を行います。", "多様なバックグラウンドを受け入れます。", 
    "常に効率的な方法を模索します。", "妥協なき品質追求を行います。"
]

VALUES_SUBJECTS = [
    "困難な状況でも", "チーム内での対立があった時に", "新しい技術選定の場面で", "顧客からのクレーム対応で", 
    "納期間際で焦っている時に", "誰かがミスをした時に", "大きな意思決定をする際に", "日々の業務の中で", 
    "後輩を指導する時に", "他部署と連携する時に", "仕様変更が起きた時に", "障害対応の最中に",
    "未経験のタスクに挑む時に", "フィードバックを受けた時に", "目標設定の面談で"
]

VALUES_ACTIONS = [
    "嘘をつかずに真実を伝えた", "自分の意見を押し通さずに傾聴した", "リスクを恐れずに新しい方法を試した", 
    "ユーザーの利益を最優先に行動した", "完璧さよりもスピードを優先してリリースした", 
    "犯人探しをせずに仕組みの改善を提案した", "感情ではなくデータに基づいて判断した", 
    "相手へのリスペクトを忘れずに接した", "教えるだけでなく共に学ぶ姿勢を見せた", "自分のタスクを超えて手助けした",
    "隠さずにすぐに報告した", "前例にとらわれずに提案した", "自ら手を挙げて担当した",
    "感謝の言葉を口に出して伝えた", "健康を第一に考えて休む決断をした"
]

VALUES_RESULTS = [
    "ことが、信頼獲得に繋がったと思います", "経験が、自分を大きく成長させてくれました", 
    "姿勢が、チームの雰囲気を良くしました", "結果、プロジェクトが成功しました", 
    "ことが、今の自分の軸になっています", "おかげで、大きなトラブルを回避できました", 
    "という体験を、もっと多くの人に伝えたいです", "瞬間が、一番印象に残っています",
    "おかげで、顧客からの評価が上がりました", "ことで、スムーズに解決できました"
]

# Q3 Expansion
Q3_PREFIXES = [
    "これからは、", "改めて考えると、", "まずは、", "チームとして、", "私たちには、", 
    "組織全体で、", "もっと強く、", "今こそ、", "リーダー層が率先して、", "新入社員も含めて、",
    "開発部全体で、", "ビジネスサイドも含めて、", "会社として、", "日々の行動として、", "会議の場でも、"
]

Q3_TEMPLATES = [
    "「{}」という文化がもっと浸透すれば良い。", 
    "全員が「{}」を実践すれば最強の組織になる。", 
    "「{}」こそが今の私たちに必要な要素です。", 
    "評価制度でも「{}」を重視すべき。", 
    "「{}」を体現している人を称賛したい。",
    "「{}」という価値観を再定義すべきだと思います。",
    "言葉だけでなく行動で「{}」を示していきたい。",
    "ミーティングの前に「{}」を確認し合いたい。",
    "採用基準に「{}」を含めるべきです。",
    "「{}」がないと成長が止まってしまう。",
    "常に「{}」を問いかける習慣をつけたい。",
    "「{}」に基づいたフィードバックを増やそう。"
]

Q3_SUFFIXES = [
    "そうすればもっと良くなるはず。", "それが一番の近道です。", "と切に願っています。", 
    "という風土を作っていきましょう。", "それが私の提案です。", "と信じています。",
    "やってみる価値はあると思います。", "から始めてみませんか？", "という意識改革が必要です。",
    "でなければ生き残れません。", "が成功の鍵です。", "に立ち返るべきです。"
]

# --- Small Voice & Dense Cluster Definitions ---

SMALL_VOICES = {
    "project": [
        "部長が経費を私的流用しているという噂を耳にしました。誰も指摘できません。",
        "役員への報告資料が、現場の数字と全く異なるポジティブな内容に書き換えられています。",
        "競合他社に機密情報が流出している形跡がありますが、隠蔽されています。",
        "特定のマネージャーによる露骨なパワハラがあり、今月だけで3人辞めました。"
    ],
    "product": [
        "新機能に致命的な脆弱性を見つけましたが、スケジュールのために無視してリリースするようです。",
        "UIが特定の障害者に全く配慮されておらず、差別的な設計になっています。",
        "課金ロジックにバグがあり、ユーザーから過剰に徴収している可能性があります。",
        "競合のソースコードをそのままコピーして使っている箇所があるという話を聞きました。"
    ],
    "welfare": [
        "給湯室にカメラが設置されており、社員の会話が監視されているようです。怖いです。",
        "サービス残業を強要され、タイムカードを早めに切るよう指示されています。",
        "社内のハラスメント相談窓口が、実は人事部長に直通で秘密が守られません。",
        "オフィスビルの耐震性に問題があるという内部報告書が無視されています。"
    ],
    "tech": [
        "顧客のクレジットカード情報が、暗号化されずにプレーンテキストでDBに保存されています。",
        "メインシステムのソースコードが外部の公開リポジトリに誤ってアップロードされています。",
        "基幹システムのバックアップが1年以上一度も成功しておらず、崩壊寸前です。",
        "開発環境へのアクセス権が退職者にも残ったままになっており、非常に危険です。"
    ],
    "values": [
        "経営陣の唱える『心理的安全性』は、イエスマンだけが生き残るための隠れ蓑です。",
        "『多様性』と言いながら、実際は特定の大学出身者だけで評価が独占されています。",
        "会社が掲げる『誠実さ』の裏で、下請け業者に不当な値下げを強要しています。",
        "失敗を歓迎すると言いながら、プロジェクトが失敗した瞬間に担当者が解雇されました。"
    ]
}

DENSE_CLUSTERS = {
    "project": [
        {"topic": "会議過多", "templates": [
            # Negative
            "会議が多すぎて作業時間がありません。", "無駄な定例を減らすべきです。", "1日中Zoomに繋ぎっぱなしで疲弊しています。", 
            "会議のための資料作成に時間を取られすぎています。", "定例会議のアジェンダが事前に共有されないため、時間が無駄になっています。", 
            "意思決定のための会議なのか、報告のための会議なのか曖昧です。",
            # Positive
            "会議が効率化されて時間が有効活用できています。", "定例会議のおかげで情報共有がスムーズです。",
            # Neutral
            "会議の数は標準的だと思います。"
        ]},
        {"topic": "リーダーシップ不足", "templates": [
            # Negative
            "リーダーが方針を示してくれません。", "責任の所在が不明確で誰も決めません。", 
            "トラブル時のエスカレーションフローが機能していません。", "マネージャーが現場の状況を把握していません。",
            "ビジョンの共有がなく、目の前のタスクをこなすだけになっています。",
            # Positive
            "リーダーの方針が明確で動きやすいです。", "マネージャーのサポートが手厚くて助かっています。",
            # Neutral
            "リーダーシップは標準的なレベルだと思います。"
        ]},
        {"topic": "工数見積もり", "templates": [
            # Negative
            "見積もりが甘すぎて残業続きです。", "営業がエンジニアに確認せず勝手に納期を決めてきます。", 
            "バッファのないスケジュールで品質が犠牲になっています。", "仕様変更が考慮されていない見積もりを押し付けられます。",
            "実績ベースでの見積もり見直しが行われていません。",
            # Positive
            "見積もりが適切で余裕を持って開発できています。", "スケジュールに余裕があり品質を保てています。",
            # Neutral
            "見積もりの精度は平均的です。"
        ]},
        {"topic": "情報共有不足", "templates": [
            # Negative
            "決定事項が現場に降りてきません。", "他部署が何をしているのか全くわかりません。", 
            "ドキュメントが更新されておらず、口頭伝承になっています。", "情報の透明性が低く、噂レベルの話しか聞こえてきません。",
            "Slackでの議論が流れてしまい、後から経緯を追うのが不可能です。", "Wikiの検索性が悪く、必要な情報が見つかりません。",
            # Positive
            "情報共有がスムーズで助かっています。", "ドキュメントが整備されていて分かりやすいです。",
            # Neutral
            "情報共有は標準的なレベルです。"
        ]},
        {"topic": "評価制度への不満", "templates": [
            # Negative
            "定性的な評価基準が曖昧で、納得感がありません。", "成果を出しても給与に反映されにくいと感じます。",
            "360度評価の結果がフィードバックされず、改善点がわかりません。", "マネージャーによって評価の厳しさにばらつきがあります。",
            # Positive
            "評価基準が明確で納得感があります。", "成果がしっかり給与に反映されています。",
            # Neutral
            "評価制度は一般的なレベルです。"
        ]}
    ],
    "product": [
        {"topic": "ドキュメント不備", "templates": [
            "仕様書が存在せず、実装が勘頼みになっています。", "要件定義がコロコロ変わり、手戻りが多いです。", 
            "最新の仕様がどこにあるのか誰も知りません。", "ドキュメント管理を徹底してください。",
            "Figmaのデザインと実際の仕様に乖離があり、混乱しています。"
        ]},
        {"topic": "UX改善", "templates": [
            "画面遷移が複雑でユーザーが迷っています。", "もっと直感的なUIにする必要があります。", 
            "モバイルでの操作性が著しく悪いです。", "デザインの一貫性がなく、使いにくいです。",
            "ロード時間が長く、ユーザーの離脱要因になっています。", "エラーメッセージが不親切で、どう対応すればいいかわかりません。"
        ]},
        {"topic": "アクセシビリティ", "templates": [
            "スクリーンリーダー対応がされていません。", "色覚特性への配慮が欠けており、見づらい配色があります。", 
            "キーボードだけで操作できない画面があります。", "フォントサイズが固定で拡大できません。",
            "画像の代替テキスト（alt）が不適切です。"
        ]},
        {"topic": "多言語対応", "templates": [
            "英語の翻訳が機械翻訳そのままで不自然です。", "日時のフォーマットが国ごとに考慮されていません。", 
            "RTL（右から左へ書く言語）のレイアウトが崩れています。", "通貨の表示がおかしいです。"
        ]},
        {"topic": "検索機能", "templates": [
            "検索精度が悪く、目的の商品が見つかりません。", "あいまい検索に対応してほしいです。", 
            "検索結果のフィルタリング機能が使いにくいです。", "サジェスト機能が的外れな候補を出します。"
        ]}
    ],
    "welfare": [
        {"topic": "CI/CD遅延", "templates": [
            "CIが遅すぎて開発フローが詰まっています。", "デプロイ待ちで1時間無駄にしました。", 
            "ビルド時間の短縮に投資してください。", "GitHub Actionsの待ち時間がストレスです。",
            "テストの並列実行をもっと増やすべきです。"
        ]},
        {"topic": "PCスペック不足", "templates": [
            "メモリ8GBではDockerが重くて動きません。", "開発マシンのスペックを上げてください。", 
            "古いPCを使わされており、生産性が低いです。", "M3 Macへのリプレイスを希望します。", 
            "モニターが1枚しかなく、作業効率が悪いです。"
        ]},
        {"topic": "リモートワーク環境", "templates": [
            "VPNが頻繁に切断され、業務に支障が出ています。", "リモート手当をもっと充実させてほしいです。",
            "出社回帰の方針について、もっと議論したいです。", "ハイブリッドワークのルールを明確にしてほしいです。"
        ]},
        {"topic": "スキルアップ支援", "templates": [
            "技術書の購入補助制度を作ってください。", "カンファレンス参加費を会社負担にしてほしいです。", 
            "業務時間内に学習する時間を認めてください。", "資格取得の手当を充実させてほしいです。",
            "社内勉強会のランチ代補助があれば活性化すると思います。"
        ]},
        {"topic": "健康管理", "templates": [
            "健康診断のオプション検診の補助を増やしてほしいです。", "産業医との面談予約が取りにくいです。", 
            "メンタルヘルスケアの窓口をもっと周知すべきです。", "インフルエンザ予防接種を社内で受けられるようにしてほしいです。"
        ]}
    ],
    "tech": [
        {"topic": "技術的負債", "templates": [
            "レガシーコードが複雑すぎて修正できません。", "テストコードがなく、変更が怖いです。", 
            "継ぎ接ぎの改修でアーキテクチャが破綻しています。", "リファクタリングの時間を確保させてください。",
            "古いライブラリの依存関係が足かせになっています。", "スパゲッティコード化したクラスを分割したいです。"
        ]},
        {"topic": "エラー頻発", "templates": [
            "本番環境で謎のエラーが頻発しています。", "エラーハンドリングが統一されていません。", 
            "ログ監視が不十分で原因特定に時間がかかります。", "システムが不安定で、夜間対応が増えています。",
            "Sントリーのエラー通知が多すぎて、オオカミ少年状態です。"
        ]},
        {"topic": "セキュリティ", "templates": [
            "脆弱性診断を定期的に実施すべきです。", "パスワードポリシーが古く、2要素認証を強制すべきです。", 
            "S3バケットの公開設定が適切か不安です。", "個人情報の扱いに関するガイドラインが守られていません。",
            "退職者のアカウント削除が漏れていることがあります。"
        ]},
        {"topic": "インフラコスト", "templates": [
            "AWSのコストが無駄にかかっています。", "開発環境のインスタンスを夜間停止すべきです。", 
            "ログの保持期間が長すぎてストレージを圧迫しています。", "リザーブドインスタンスの活用を検討してください。",
            "CDNのキャッシュ設定を見直して転送料を削減しましょう。"
        ]},
        {"topic": "ドキュメントツール", "templates": [
            "Notionをもっと活用して情報を一元化したいです。", "Wikiが散らかりすぎて検索できません。", 
            "APIドキュメントをSwaggerで自動生成するようにしたいです。", "議事録のフォーマットを統一しましょう。"
        ]}
    ],
    "values": [
        {"topic": "称賛文化", "templates": [
            "私たちは互いに称賛し合う文化を作ります。", "感謝を伝える仕組み（Thanksカードなど）を積極的に活用します。", 
            "成果だけでなくプロセスも評価されるべきです。", "ポジティブなフィードバックが飛び交う組織にします。",
            "他チームの成功事例を全力で祝いましょう。", "日々の小さな貢献にもスポットライトを当てたいです。",
            "称賛の言葉が自然に出るような雰囲気が理想です。", "Slackでの『お疲れ様』やスタンプを大切にしたいです。"
        ]},
        {"topic": "挑戦", "templates": [
            "新しい技術への挑戦を常に推奨します。", "失敗を恐れずにチャレンジできる環境こそが重要です。", 
            "守りに入らず、攻めの姿勢を貫きます。", "イノベーションを起こすための余白を大切にします。",
            "新しいツールの導入ハードルを下げ、挑戦を加速させます。", "未知の領域に踏み出す勇気を組織全体で応援しましょう。",
            "『まずはやってみる』というマインドを浸透させたいです。", "失敗から学ぶことを評価する仕組みが必要です。"
        ]},
        {"topic": "心理的安全性", "templates": [
            "ミスを責めるのではなく、仕組みで解決します。", "反対意見も歓迎される雰囲気を守ります。",
            "若手が自由に発言できる環境を作ります。", "心理的安全性が高いチームは、結果として生産性も高いはずです。",
            "間違いを隠すのではなく、すぐに共有できる誠実さを持ちたいです。", "誰が言ったかではなく、何を言ったかを重視する組織にしたいです。",
            "遠慮せずにフィードバックを送り合える関係が理想です。"
        ]},
        {"topic": "顧客志向", "templates": [
            "ユーザーの声を直接聞く機会を最優先します。", "ドッグフーディングを徹底し、ユーザー視点を忘れません。", 
            "売上よりもユーザー満足度を追求します。", "カスタマーサポートからのフィードバックを宝として扱います。",
            "ペルソナに基づいた開発を徹底します。", "真の顧客価値とは何かを常に問い続けたいです。",
            "ユーザーの期待を超える驚きを提供し続けましょう。", "クライアントの成功が私たちのやりがいです。"
        ]},
        {"topic": "スピード", "templates": [
            "意思決定のスピードを上げ、競合に勝ちます。", "完璧を目指すより、まずリリースして学びます。", 
            "承認プロセスを簡略化し、スピードを落としません。", "アジャイルな開発プロセスを徹底的に実践します。",
            "リードタイムの短縮が最大の競争優位性になります。", "まずは小さく始めて、素早く検証する文化を定着させたいです。",
            "迅速なフィードバックループを回して、プロダクトを磨き上げましょう。"
        ]}
    ]
}

def get_small_voice_comment(category="project"):
    """Returns a specific outlier comment."""
    cat = category if category in SMALL_VOICES else "project"
    return random.choice(SMALL_VOICES[cat])

def get_dense_cluster_comment(category="project"):
    """Returns a comment from a specific topic cluster."""
    cat = category if category in DENSE_CLUSTERS else "project"
    cluster = random.choice(DENSE_CLUSTERS[cat])
    return random.choice(cluster["templates"])

# --- Data Generation Logic ---

def get_project_comment():
    rand = random.random()
    if rand < 0.60:  # 60% negative
        parts = [INTROS, PROJ_SUBJECTS, PROJ_ACTIONS, PROJ_REASONS, CLOSINGS]
    elif rand < 0.85:  # 25% neutral (0.60 + 0.25 = 0.85)
        parts = [INTROS, PROJ_SUBJECTS, PROJ_ACTIONS_NEUTRAL, PROJ_REASONS_NEUTRAL, CLOSINGS]
    else:  # 15% positive
        parts = [INTROS, PROJ_SUBJECTS, PROJ_ACTIONS_POS, PROJ_REASONS_POS, CLOSINGS]
    return generate_combinatorial_sentence(parts)

def get_dev_env_comment():
    rand = random.random()
    if rand < 0.60:  # 60% negative
        parts = [INTROS, DEV_SUBJECTS, DEV_ACTIONS, DEV_REASONS, CLOSINGS]
    elif rand < 0.85:  # 25% neutral
        parts = [INTROS, DEV_SUBJECTS, DEV_ACTIONS_NEUTRAL, DEV_REASONS_NEUTRAL, CLOSINGS]
    else:  # 15% positive
        parts = [INTROS, DEV_SUBJECTS, DEV_ACTIONS_POS, DEV_REASONS_POS, CLOSINGS]
    return generate_combinatorial_sentence(parts)

def get_tech_quality_comment():
    rand = random.random()
    if rand < 0.60:  # 60% negative
        parts = [INTROS, TECH_SUBJECTS, TECH_ACTIONS, TECH_REASONS, CLOSINGS]
    elif rand < 0.85:  # 25% neutral
        parts = [INTROS, TECH_SUBJECTS, TECH_ACTIONS_NEUTRAL, TECH_REASONS_NEUTRAL, CLOSINGS]
    else:  # 15% positive
        parts = [INTROS, TECH_SUBJECTS, TECH_ACTIONS_POS, TECH_REASONS_POS, CLOSINGS]
    return generate_combinatorial_sentence(parts)

def get_value_comment_q1():
    return random.choice(VALUES_THEMES)

def get_value_comment_q2():
    start = ["以前、", "昔、", "入社当時に、", "前職で、", "先日、", "あるプロジェクトで、", "新人の頃、", "リーダーになった時に、", "", ""]
    parts = [start, VALUES_SUBJECTS, VALUES_ACTIONS, VALUES_RESULTS]
    return generate_combinatorial_sentence(parts)

def get_value_comment_q3(index=0):
    prefix = random.choice(Q3_PREFIXES) if random.random() > 0.3 else ""
    val = random.choice(VALUES_THEMES)
    tmpl = random.choice(Q3_TEMPLATES).format(val)
    suffix = random.choice(Q3_SUFFIXES) if random.random() > 0.3 else ""
    comment = f"{prefix}{tmpl} {suffix}".strip()
    return comment

DEPARTMENTS = ["SREチーム", "フロントエンド", "バックエンド", "モバイルアプリ", "QAチーム", "デザイン", "PdM", "データ分析"]

def generate_mixed_comments_list(count, category):
    """
    Generates a list of UNIQUE comments with balanced cluster distribution:
    - 5% Small Voice (Outliers) - Clear outliers that stand out
    - 80% Dense Clusters - BALANCED (each cluster gets roughly equal comments)
    - 15% General Noise - For diversity
    
    IMPORTANT: All comments are unique, creating distinct opinions within each cluster
    """
    n_small_voices = max(int(count * 0.05), 1)
    n_dense_clusters = int(count * 0.80)
    n_general = count - n_small_voices - n_dense_clusters
    
    comments = []
    seen = set()
    
    # Get generator function based on category
    gen_func = get_project_comment
    if category == "product": gen_func = get_tech_quality_comment 
    elif category == "welfare" or category == "dev_env": gen_func = get_dev_env_comment
    elif category == "tech": gen_func = get_tech_quality_comment
    elif category == "values": gen_func = get_value_comment_q3
    
    # 1. ADD SMALL VOICES (Rare outliers) - ensure uniqueness
    cat = category if category in SMALL_VOICES else "project"
    available_small_voices = SMALL_VOICES[cat].copy()
    random.shuffle(available_small_voices)
    
    for i in range(min(n_small_voices, len(available_small_voices))):
        comment = available_small_voices[i]
        if comment not in seen:
            comments.append(comment)
            seen.add(comment)
        
    # 2. ADD DENSE CLUSTERS - WITH BALANCED DISTRIBUTION
    cat = category if category in DENSE_CLUSTERS else "project"
    available_clusters = DENSE_CLUSTERS[cat]
    
    if available_clusters:
        # Create BALANCED distribution: each cluster gets roughly equal comments
        num_clusters = len(available_clusters)
        comments_per_cluster = n_dense_clusters // num_clusters
        remainder = n_dense_clusters % num_clusters
        
        # Assign comments to clusters evenly
        cluster_assignments = []
        for i, cluster in enumerate(available_clusters):
            # Give each cluster equal share, plus 1 extra for first 'remainder' clusters
            cluster_count = comments_per_cluster + (1 if i < remainder else 0)
            cluster_assignments.extend([cluster] * cluster_count)
        
        random.shuffle(cluster_assignments)
        
        # Generate UNIQUE comments from assigned clusters
        # Track which templates we've used from each cluster
        cluster_template_indices = {i: list(range(len(cluster["templates"]))) for i, cluster in enumerate(available_clusters)}
        for cluster_idx, cluster in enumerate(available_clusters):
            random.shuffle(cluster_template_indices[cluster_idx])
        
        for cluster in cluster_assignments[:n_dense_clusters]:
            cluster_idx = available_clusters.index(cluster)
            templates = cluster["templates"]
            
            # Try to get an unused template from this cluster
            max_attempts = 50
            for attempt in range(max_attempts):
                if cluster_template_indices[cluster_idx]:
                    # Use next template in shuffled order
                    template_idx = cluster_template_indices[cluster_idx].pop(0)
                    comment = templates[template_idx]
                    
                    # If we've exhausted templates, reshuffle and reuse with variation
                    if not cluster_template_indices[cluster_idx]:
                        cluster_template_indices[cluster_idx] = list(range(len(templates)))
                        random.shuffle(cluster_template_indices[cluster_idx])
                else:
                    # Fallback: pick random template
                    comment = random.choice(templates)
                
                # Add variation if duplicate
                if comment in seen:
                    # Add subtle variation
                    variations = ["", "。", "です。", "ます。", "と思います。", "と感じます。"]
                    comment = comment.rstrip("。") + random.choice(variations)
                
                if comment not in seen:
                    comments.append(comment)
                    seen.add(comment)
                    break
            else:
                # If still can't find unique, generate from general function
                for _ in range(20):
                    comment = gen_func()
                    if comment not in seen:
                        comments.append(comment)
                        seen.add(comment)
                        break
    
    # 3. ADD GENERAL NOISE - ensure uniqueness
    attempts = 0
    max_attempts = n_general * 10
    while len(comments) < count and attempts < max_attempts:
        comment = gen_func()
        if comment not in seen:
            comments.append(comment)
            seen.add(comment)
        attempts += 1
    
    # Fill any remaining slots if we couldn't generate enough unique comments
    while len(comments) < count:
        comment = gen_func() + f" (バリエーション{len(comments)})"
        if comment not in seen:
            comments.append(comment)
            seen.add(comment)
    
    random.shuffle(comments)
    return comments[:count]

def generate_csv_files():
    # 1. Project Management
    print("Generating Project Management data...")
    # Apply "project" category logic to both columns roughly
    project_comments_1 = generate_mixed_comments_list(NUM_ROWS_OTHERS, "project")
    project_comments_2 = generate_mixed_comments_list(NUM_ROWS_OTHERS, "project")
    
    project_data = []
    for i in range(NUM_ROWS_OTHERS):
        project_data.append({
            "日時": generate_random_date(),
            "部署": random.choice(DEPARTMENTS),
            "プロセスへのコメント": project_comments_1[i],
            "会議へのコメント": project_comments_2[i]
        })
    pd.DataFrame(project_data).to_csv(os.path.join(OUTPUT_DIR, "project.csv"), index=False, encoding="utf-8-sig")

    # 2. Dev Environment
    print("Generating Dev Environment data...")
    dev_comments_1 = generate_mixed_comments_list(NUM_ROWS_OTHERS, "welfare") # Use welfare/dev_env category
    dev_comments_2 = generate_mixed_comments_list(NUM_ROWS_OTHERS, "welfare")

    dev_data = []
    for i in range(NUM_ROWS_OTHERS):
        dev_data.append({
            "日時": generate_random_date(),
            "部署": random.choice(DEPARTMENTS),
            "ハードウェアへのコメント": dev_comments_1[i],
            "ソフトウェアへのコメント": dev_comments_2[i]
        })
    pd.DataFrame(dev_data).to_csv(os.path.join(OUTPUT_DIR, "dev_env.csv"), index=False, encoding="utf-8-sig")

    # 3. Tech Quality
    print("Generating Tech Quality data...")
    tech_comments_1 = generate_mixed_comments_list(NUM_ROWS_OTHERS, "tech")
    tech_comments_2 = generate_mixed_comments_list(NUM_ROWS_OTHERS, "tech")

    tech_data = []
    for i in range(NUM_ROWS_OTHERS):
        tech_data.append({
            "日時": generate_random_date(),
            "部署": random.choice(DEPARTMENTS),
            "コードベースへのコメント": tech_comments_1[i],
            "テストへのコメント": tech_comments_2[i]
        })
    pd.DataFrame(tech_data).to_csv(os.path.join(OUTPUT_DIR, "tech_quality.csv"), index=False, encoding="utf-8-sig")

    # 4. Values (Large dataset)
    print("Generating Values data...")
    
    q2_list = generate_unique_sentence_list(get_value_comment_q2, NUM_ROWS_VALUES)
    q3_list = generate_mixed_comments_list(NUM_ROWS_VALUES, "values")
    
    values_data = []
    for i in range(NUM_ROWS_VALUES):
        values_data.append({
            "日時": generate_random_date(),
            "部署": random.choice(DEPARTMENTS),
            "価値観のテーマ": get_value_comment_q1(), 
            "きっかけのエピソード": q2_list[i],
            "提案する価値観": q3_list[i]
        })
    pd.DataFrame(values_data).to_csv(os.path.join(OUTPUT_DIR, "values.csv"), index=False, encoding="utf-8-sig")

    print(f"✅ Generated data: All files have {NUM_ROWS_VALUES} rows.")



# --- Main ---
if __name__ == "__main__":
    if os.path.exists(OUTPUT_DIR):
        shutil.rmtree(OUTPUT_DIR)
        print(f"Cleaned directory: {OUTPUT_DIR}")
    
    os.makedirs(OUTPUT_DIR)
    print(f"Created directory: {OUTPUT_DIR}")

    print("🚀 Starting engineer-focused data generation...")
    
    generate_csv_files()

    
    print("\n✨ Data generation completed!")
